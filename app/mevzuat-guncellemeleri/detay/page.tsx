import LegacyMevzuatGuncellemeDetayRedirect from "@/components/LegacyMevzuatGuncellemeDetayRedirect";

/** Eski `?slug=` bağlantılarını yeni path tabanlı URL'ye yönlendirir. */
export default function LegacyMevzuatGuncellemeDetayPage() {
  return <LegacyMevzuatGuncellemeDetayRedirect />;
}
